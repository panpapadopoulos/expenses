# This app has no build-time secrets — the frontend never talks to anything
# other than its own same-origin /api/data endpoint.
#
# Real secrets (the login PASSWORD, the Cloudflare KV binding) are configured
# on the Cloudflare Worker itself (dashboard or `wrangler secret put`), never
# committed here. See README.md "Deployment" for the exact steps.
